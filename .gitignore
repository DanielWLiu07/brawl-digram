# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
dist/
build/
.next/
.turbo/

# Env
.env
.env.local
.env.*.local

# Claude Code local settings (personal overrides, never commit)
.claude/settings.local.json

# Logs
*.log

# Raw APK texture dumps (500MB+) — keep the extraction OUTPUT (data/tile_sprites/), not the source dumps
data/sc_textures/
logs/

# Liquipedia raw wikitext cache (regenerable via data/fetch_liquipedia_drafts.py)
data/liquipedia_cache/

# Local planning / scratch docs — never tracked
plans/
